{
  "name": "better-playlists-test-backend",
  "description": "the backend for better-playlists-test"
  "version": "1.0.0",
  "engines": {
    "node": "9.4.0",
    "npm": "5.6.0"
  },
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.15.4",
    "querystring": "^0.2.0",
    "request": "^2.81.0"
  }
}
